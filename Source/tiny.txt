inteiro : c
inteiro foo(inteiro: a, inteiro: b)
    
    {inteiro: b}
    se a então
        car := 10
        se a >= b então
            variavel := (a+b)
        senão   
            variavel := 1*(a+b)
            repita
                var := var + 1
            ate n < 0
        fim
    fim
    
    repita
        var := var + 1
        repita
            var := 15
        ate 1
    ate n < 0

    leia(abc)

    flutuante: k

    escreve(foo(a,b))

    retorna(2*5)

fim

inteiro: j

vazio teste()
    
    var := 10
    
    se a então
        car := 10
        se a >= b então
            variavel := (a+b)
        senão   
            variavel := 1*(a+b)
            repita
                var := var + 1
            ate n < 0
        fim
    fim
fim